on:
  workflow_dispatch:

jobs:
  run_app:
    name: Run APp
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: run-command
        run: python main.py
